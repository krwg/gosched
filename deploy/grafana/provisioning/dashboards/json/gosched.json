{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list" }, "tooltip": { "mode": "single" } },
      "targets": [
        {
          "expr": "sum by (algorithm, status) (gosched_schedules_total)",
          "legendFormat": "{{algorithm}} {{status}}",
          "refId": "A"
        }
      ],
      "title": "Schedules total",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "ms" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "gosched_average_wait_ms",
          "legendFormat": "{{algorithm}}",
          "refId": "A"
        }
      ],
      "title": "Average wait (ms)",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "percent" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        {
          "expr": "gosched_cpu_utilization_pct",
          "legendFormat": "{{algorithm}}",
          "refId": "A"
        }
      ],
      "title": "CPU utilization %",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "targets": [
        {
          "expr": "gosched_tasks_completed_total",
          "legendFormat": "completed",
          "refId": "A"
        },
        {
          "expr": "gosched_tasks_missed_total",
          "legendFormat": "missed",
          "refId": "B"
        }
      ],
      "title": "Tasks completed / missed",
      "type": "timeseries"
    }
  ],
  "refresh": "5s",
  "schemaVersion": 39,
  "tags": ["gosched", "scheduler"],
  "templating": { "list": [] },
  "time": { "from": "now-15m", "to": "now" },
  "title": "GoSched",
  "uid": "gosched-main",
  "version": 1
}
